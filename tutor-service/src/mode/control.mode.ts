export enum ControlMode {
    SOFT = 'soft',
    FORCE = 'force',
}

export enum Period {
    HOUR = 'hour',
    DAY = 'day'
}

export enum DuplicatingObject {
    RESOURCE = 'resource',
    SCHEDULE = 'schedule',
    PLAN = 'plan'
}