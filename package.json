{
  "scripts": {
    "fapi": "cd adaptive-learning-service && python -m venv venv && .\\venv\\Scripts\\activate && pip install -r requirements.txt && uvicorn main:app --reload",
    "fe": "cd frontend && npm install && npm run dev",
    "be": "cd tutor-service && npm install && npm run start:dev",
    "be:all": "npm-run-all --parallel fapi be",
    "start:all": "npm-run-all --parallel fapi be fe"
  },
  "dependencies": {
    "npm-run-all": "^4.1.5"
  }
}
